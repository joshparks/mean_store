var User = require('./models/user');

module.exports = function(app) {

	// register a user
	app.post('/api/register', function(req, res) {

    	
    });

    // login the user
    app.post('/api/login', function(req, res) {

    	
    });

    // retrieve user profile
    app.get('/api/profile/:todo_id', function(req, res) {

    	
    	
    });

};